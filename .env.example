# Copy to .env and paste your token. .env is git-ignored and auto-loaded by
# `python run.py ...` and `python run.py serve` (via arena.runner.load_env).
# Create a GitHub PAT with the "models:read" scope: https://github.com/settings/tokens
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxx
